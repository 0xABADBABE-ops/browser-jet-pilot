---
title: "Architecture"
description: "System architecture for Browser Jet Pilot, BrowserAgent, BrowserSkill, and MCP clients."
keywords: ["architecture", "mermaid", "mcp", "playwright", "cdp"]
---

## System flow

```mermaid
flowchart LR
  subgraph App["Your Application"]
    BS["BrowserSkill"]
    BA["BrowserAgent (AI)"]
    MC["MCP Client<br/>(Claude Desktop, Cursor, etc.)"]
  end

  BS -->|MCP JSON-RPC| MB["Browser Jet Pilot<br/>(stdio or HTTP)"]
  BA -->|MCP JSON-RPC| MB
  MC -->|MCP JSON-RPC| MB

  MB --> PW["Playwright"]
  PW -->|CDP| CH["Your Chromium / Chrome"]
```

## Notes

- `BrowserSkill` is the stable integration layer for framework-driven execution.
- `BrowserAgent` adds optional LLM planning on top of deterministic tools.
- Any MCP-compatible client can call the same tool surface through `/mcp` (HTTP) or stdio.
- Browser control always resolves to Playwright + CDP, so runtime behavior is consistent across integration modes.

